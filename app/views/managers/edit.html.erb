<h1>Editing Manager</h1>

<%= render 'form', manager: @manager %>

<%= link_to 'Show', @manager, class: 'btn btn-primary' %> 
<%= link_to 'Back', team_managers_path(@manager.team), class: 'btn btn-primary' %>
