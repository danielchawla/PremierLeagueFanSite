<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :hometeam_id %>
    <%= form.collection_select(:hometeam_id, Team.all, :id, :full_name) %>
  </div>

  <div class="field">
    <%= form.label :awayteam_id %>
    <%= form.collection_select(:awayteam_id, Team.all, :id, :full_name) %>
  </div>

  <div class="field">
    <%= form.label :hometeamscore %>
    <%= form.number_field :hometeamscore %>
  </div>

    <div class="field">
    <%= form.label :awayteamscore %>
    <%= form.number_field :awayteamscore %>
  </div>

  <div class="field">
    <%= form.label :gamedate %>
    <%= form.date_field :gamedate %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
